import { StyleSheet, Text, View } from "react-native";
import React from "react";

const InternetService = () => {
  return (
    <View>
      <Text>InternetService</Text>
    </View>
  );
};

export default InternetService;

const styles = StyleSheet.create({});
